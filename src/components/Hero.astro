---
const { title } = Astro.props;
---

<div class="absolute top-4/10 left-20">
    <div class="mx-auto text-center relative">
        <h1
            class="absolute z-5 top-1/2 text-[75px] w-250 uppercase text-start [-webkit-text-stroke:1px_black] font-lilitaone text-white mb-4 leading-20"
        >
            {title}
        </h1>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const scrollArrow = document.getElementById("scroll-down-arrow");
        if (scrollArrow) {
            scrollArrow.addEventListener("click", () => {
                window.scrollBy({
                    top: window.innerHeight,
                    behavior: "smooth",
                });
            });
        }
    });
</script>