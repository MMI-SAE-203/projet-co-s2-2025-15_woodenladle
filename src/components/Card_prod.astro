---
const {...prod} = Astro.props;
---

<div class="bg-white shadow-md rounded-lg overflow-hidden mb-4">
    <img src={prod.image} alt={prod.title} class="w-full h-48 object-cover" />
    <div class="p-4">
        <h2 class="text-xl font-bold mb-2">{prod.title}</h2>
        <p class="text-gray-700 mb-4">{prod.description}</p>
        <a href={prod.link} class="text-blue-500 hover:underline">Learn more</a>
    </div>